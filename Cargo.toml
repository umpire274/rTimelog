[package]
name = "rTimelog"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.37.0", features = ["bundled"] }
chrono = "0.4.42"        # per data e orari
clap = { version = "4.5.47", features = ["derive"] }  # per CLI
